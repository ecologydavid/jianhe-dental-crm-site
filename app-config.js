// Public connection settings shared by every device. Never add secret/service keys.
// Fill after the clinic Supabase project has been selected and initialized.
window.CLINIC_CONFIG = Object.freeze({ supabaseUrl: '', supabasePublishableKey: '' });
